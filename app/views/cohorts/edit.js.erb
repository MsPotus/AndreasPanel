//confirm I am in this file
console.log('I am hitting the js file for edit')

//Grab the specific place at which the cohort I want to edit exists
cohortdiv = document.getElementById('cohort-<%= @cohort.id%>')

//console log that task that I clicked on 
console.log("Current Cohort clicked " + cohortdiv.innerText)

//Gave up here and had to use JQuery to replace the TR element becuase trs dont like forms inside of them 
$(cohortdiv).replaceWith("<%= j (render 'form') %>");