<%= form_for course,  html: {class: "nifty_form"} do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %>
  <%= f.label :hours %>:
  <%= f.text_field :hours %>
  <%= f.submit "Create" %>
<% end %>