
<p id="notice"><%= notice %></p>
 
<p>
<strong>Name</strong>
<%= @course.name %>
</p>


<h2>Cohorts</h2>
<% @course.cohorts.all.each do |cohort| %>
    <p>
        <%= cohort.name  %>
    </p>
<% end %>
<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', course_path %>
